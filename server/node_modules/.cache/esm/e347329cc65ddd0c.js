let express,readdirSync;_c65‍.w("express",[["default",["express"],function(v){express=v}]]);_c65‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);//const express = require("express");

//import router from './routes/auth';

// This way of importing morgan give a warning in the console
// import morgan from "morgan";


const app = express ();

// middlewares
app.use(morgan("dev"));

// Route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));
//app.use("/api", router);

// Create a port variable and access the value from the .env file
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));