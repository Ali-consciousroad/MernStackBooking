let express,showMessage,register;_fa0‍.w("express",[["default",["express"],function(v){express=v}]]);_fa0‍.w("../controllers/auth",[["showMessage",["showMessage"],function(v){showMessage=v}],["register",["register"],function(v){register=v}]]);

const router = express.Router();

// controllers

// :message is dynamic, it will allow to return whatever was written in the browser
router.get("/:message", showMessage);
rouer.post("/register", register);

// Common mistake, we can't use this syntax anymore because of the require() in the server.js file
// Export default router; 
module.exports = router;