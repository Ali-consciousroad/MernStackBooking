_793‍.x([["showMessage",()=>showMessage],["register",()=>register]]);const showMessage = (req, res) => {
    res.status(200).send(`Here is your secret message: ${req.params.message}`);
}

       const register = async (req, res) => {
    console.log(req.body);
};