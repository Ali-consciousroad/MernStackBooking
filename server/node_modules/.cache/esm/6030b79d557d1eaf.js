let express;_557‍.w("express",[["default",["express"],function(v){express=v}]]);
const router = express.Router();

// :message is dynamic, it will allow to return whatever was written in the browser
router.get("/:message", (req, res) => {
    res.status(200).send(`Here is your secret message: ${req.params.message}`);
});

// Common mistake, we can't use this syntax because we used require() in the server.js file
// Export default router; 